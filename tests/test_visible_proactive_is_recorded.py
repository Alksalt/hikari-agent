"""Codex P1 — visible proactive record tests.

The legacy maybe_send_heartbeat function was deleted in Phase J.
Coverage for proactive message persistence is now owned by the
engagement_tick + sender integration tests in test_engagement_*.py.
"""
