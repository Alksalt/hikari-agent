.venv/
.env
.envrc
__pycache__/
*.pyc
*.pyo
data/
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/
.DS_Store
secrets/

# Hikari runtime DB (real one lives at data/hikari.db; stub at root is from earlier path)
/hikari.db
hikari.db
*.db-shm
*.db-wal

# Claude Code agent worktree cache (created by Agent subagents running in isolation)
.claude/worktrees/

# /ship plan working notes (per-feature, not source of truth)
plans/

